import React from 'react';

/* function Header({titulo}) {

    return(
        <Fragment>
            <h1>{ titulo }</h1>
        </Fragment>
    )
} */

// Otra forma de crear el componente
const Header = ({titulo}) => (   
            <h1>{ titulo }</h1>   
     );

 
export default Header;

